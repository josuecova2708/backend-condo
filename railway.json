{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py check --deploy && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn smart_condo_project.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  }
}